= form_for @feature, builder: BulmaFormBuilder::HorizontalBulmaFormBuilder do |f|
  = f.errors

  = f.labeled_text_field :name, placeholder: t('name.placeholder')
  = f.labeled_text_field :icon
  .actions
    = f.submit t(':save')

